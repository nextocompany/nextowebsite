---
phase: 02-page
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
requirements: [LAYOUT-01, LAYOUT-04]
files_modified:
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor sees all 9 sections scrolling down a single page in order: Header, Hero, What we do, Focus, Products, How we work, About, Contact, Footer"
    - "Build passes clean with no TypeScript or lint errors"
    - "Layout is usable at 360px, 768px, 1024px, and 1280px viewport widths"
    - "No horizontal scrollbar at any breakpoint"
    - "3-column grids stack to single column on mobile"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Page composition with all sections in order"
      contains: "Header"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render"
      pattern: "import.*Header.*from.*layout/Header"
    - from: "src/app/page.tsx"
      to: "src/components/sections/Hero.tsx"
      via: "import and render"
      pattern: "import.*Hero.*from.*sections/Hero"
    - from: "src/app/page.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer.*from.*layout/Footer"
---

<objective>
Assemble all section components into page.tsx to create the complete single-page site, verify the build, and get human visual verification.

Purpose: This is the final assembly step — composing all 9 components in the correct order and verifying the complete page works across breakpoints.

Output: Updated page.tsx with full page composition. Human-verified responsive layout.
</objective>

<execution_context>
@/Users/nex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page/02-01-SUMMARY.md
@.planning/phases/02-page/02-02-SUMMARY.md
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose page.tsx with all sections and verify build</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Replace the current demo content in `src/app/page.tsx` with the full page composition. The file is a server component (no 'use client' — the Header handles its own client state internally).

Import all components:
```
import { Header } from '@/components/layout/Header'
import { Footer } from '@/components/layout/Footer'
import { Hero } from '@/components/sections/Hero'
import { WhatWeDo } from '@/components/sections/WhatWeDo'
import { Focus } from '@/components/sections/Focus'
import { Products } from '@/components/sections/Products'
import { HowWeWork } from '@/components/sections/HowWeWork'
import { About } from '@/components/sections/About'
import { Contact } from '@/components/sections/Contact'
```

Render in this exact order (per LAYOUT-01 requirement — 9 sections in specified order):
```
<>
  <Header />
  <main>
    <Hero />
    <WhatWeDo />
    <Focus />
    <Products />
    <HowWeWork />
    <About />
    <Contact />
  </main>
  <Footer />
</>
```

Notes:
- Remove ALL existing demo content (typography demo, button demo)
- `<main>` wraps the content sections (not Header/Footer) — semantic HTML
- No additional wrapper divs or styles needed on page.tsx — each section component handles its own container width and padding
- Do NOT add 'use client' to page.tsx — it's a server component that composes other components

After writing page.tsx, run `npm run build` to verify the production build passes clean (no TypeScript errors, no lint errors). Fix any import path issues if they arise.
  </action>
  <verify>
`npm run build` completes with exit code 0 and no errors. Run `grep -c "import" src/app/page.tsx` to verify all 9 imports are present.
  </verify>
  <done>
page.tsx imports and renders all 9 components (Header, Hero, WhatWeDo, Focus, Products, HowWeWork, About, Contact, Footer) in the specified order. Production build passes clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and responsive verification</name>
  <files>src/app/page.tsx</files>
  <action>
CHECKPOINT: Human visual verification of the complete assembled page.

Start the dev server with `npm run dev` and visit http://localhost:3000.

**Desktop verification (1280px+ viewport):**
1. Scroll down — verify all 9 sections appear in order: Header (sticky), Hero, What we do, Focus, Products, How we work, About, Contact, Footer
2. Click "Products" nav link — verify smooth scroll to Products section, heading not hidden behind header
3. Click "Contact" nav link — verify smooth scroll to Contact section
4. Scroll slowly through all sections — verify the active nav link changes (bold text) as each section enters view
5. Verify Hero shows: H1 "Nexto", subtitle "Enterprise software studio.", two body lines, two CTA buttons (Contact, Products)
6. Verify Product cards show borders, three products with titles and descriptions
7. Verify How we work shows 4 numbered steps with large numbers
8. Verify Contact shows clickable email (info@nexto.co.th) and LINE link (@nextocompany)
9. Verify Footer shows copyright and Privacy link

**Mobile verification (resize to ~375px width, or use DevTools mobile):**
10. Verify hamburger icon appears, nav links are hidden
11. Tap hamburger — verify slide-out panel from right with dark overlay
12. Tap a nav link in panel — verify menu closes and page scrolls to section
13. Verify 3-column grids (What we do, Products) stack to single column
14. Verify no horizontal scrollbar at 360px width

**Additional breakpoints:**
15. Check layout at 768px — grids should be 3 columns
16. Check layout at 1024px — content centered within max-w-5xl

Resume signal: Type "approved" or describe any issues to fix.
  </action>
  <verify>Human confirms visual and functional correctness across breakpoints.</verify>
  <done>All 9 sections visible, navigation works with smooth scroll and active indicator, mobile hamburger functions, responsive layout verified at 360/768/1024/1280px.</done>
</task>

</tasks>

<verification>
- page.tsx renders all 9 sections in correct order
- `npm run build` passes with zero errors
- Human confirms: all sections visible, nav works, responsive layout correct
- No horizontal scroll at 360px
- Smooth scrolling works with proper header offset
- Mobile hamburger menu functions correctly
</verification>

<success_criteria>
The complete single-page Nexto website is assembled and human-verified at desktop and mobile breakpoints. All 9 sections render with correct content, navigation scrolls smoothly, and layout is responsive.
</success_criteria>

<output>
After completion, create `.planning/phases/02-page/02-03-SUMMARY.md`
</output>
